namespace WorldAlerts.Domain.Enums;

/// <summary>
/// Represents the severity level of a world event.
/// </summary>
public enum EventSeverity
{
    Unknown = 0,
    Low = 1,
    Medium = 2,
    High = 3,
    Critical = 4,
    Catastrophic = 5,
}
